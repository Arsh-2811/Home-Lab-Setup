# ===============================================================
# General Exclusions
# ===============================================================
# Ignore all .env files and secrets, but allow .env.template files
*.env
.env.*
!*.env.template
*.pem
*.key
*.crt
*secret*

# Ignore OS-generated files
.DS_Store
Thumbs.db

# Ignore logs and databases
*.log
*.db
*.sqlite
*.sql
*.sql.gz
context.txt

# ===============================================================
# Service-Specific Exclusions
# ===============================================================

# --- Immich ---
# Ignore all user-generated content, media, thumbnails, backups, and database data.
# Only the docker-compose.yml should be committed.
immich/library/
immich/thumbs/
immich/upload/
immich/profile/
immich/postgres/

# --- Nginx Proxy Manager (NPM) ---
# Ignore all generated data, logs, SSL certificates, and the configuration database.
# Only the docker-compose.yml is needed.
npm/data/
npm/letsencrypt/

# --- Pi-hole ---
# Ignore the dynamic and sensitive files, but keep the core user configuration.
# This keeps your adlists, custom domains (local DNS), and main config.
pihole/etc-pihole/*
!pihole/etc-pihole/pihole.toml
!pihole/etc-pihole/adlists.list
pihole/etc-pihole/hosts/*
!pihole/etc-pihole/hosts/custom.list

# Also ignore custom dnsmasq files unless you specifically create them.
# If you add your own .conf files here, you can remove the next line.
pihole/etc-dnsmasq.d/

# --- Root Certificates ---
# Ignore the root certs directory.
certs/

# --- Reports or other local files ---
Report.md
